[Unit]
Description=Xremap key remapper (Labwc)
PartOf=labwc-session.target
After=labwc-session.target
Conflicts=xremap.service

[Service]
ExecStart=xremap /etc/xremap/default.json --watch=device,config
Restart=on-failure
RestartSec=1

[Install]
WantedBy=labwc-session.target
